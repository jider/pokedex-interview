import {css, Styles} from '../../../styled-system/css'

const baseStyles: Styles = {
  minHeight: 'calc(100vh - 3rem)',
  backgroundColor: 'slate.800',
  padding: '4',
}

export const mainContent = css(baseStyles)
